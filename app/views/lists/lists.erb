<!DOCTYPE html>
<html>
<title>W3.CSS</title>
<meta name="viewport" content="width=device-width, initial-scale=1">
<link rel="stylesheet" href="http://www.w3schools.com/lib/w3.css">
<body>

<div class="w3-container">
  <h2>Colored Table Heading</h2>
  <p>Use any of the w3-color classes to add a color to the table.</p>
  <% if !@lists.empty? %>
  <%@lists.each do |list| %>
  <table class="w3-table w3-striped w3-border">
    <thead>
      <tr class="w3-red">
          <th><%= list.name %></th>

      </tr>
    </thead>
    <tr>
      <td> <%= list.description%> </td>
    </tr>
    <tr>
      <% list.etfs.each do |etf|%>

        <td><a href="/etf/<%=etf.slug%>"><%= etf.name%></a></td>

      <% end %>
    </tr>

  </table>
  <br>
  <br>
  <br>
  <% end %>
  <% end %>
</div>

</body>
</html>
